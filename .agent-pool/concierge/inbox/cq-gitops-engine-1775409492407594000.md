---
id: cq-gitops-engine-1775409492407594000
from: gitops-engine
to: concierge
type: response
priority: normal
timestamp: 2026-04-05T17:22:44.652173Z
---

## Test Audit + Narrative Logging Complete

All changes verified: `go test ./internal/fileutil/... ./internal/reconcile/... -count=1` → all pass.

---

### Part 1: Test Audit

#### `CopyFileIfChanged` — I/O boundary function, High risk

| Path | Status | Notes |
|------|--------|-------|
| New file (dst absent) → write | ✅ Covered | |
| Identical content → skip | ✅ Covered | |
| Differing content → write | ✅ Covered | |
| Symlink src → `ErrSymlinkSkipped` → `(false, nil)` | ❌ **Gap — FIXED** | `FileHash` follows symlink; `CopyFile` detects via `Lstat` |
| Size mismatch after hash match → force write | ⚠️ Untestable | Requires FUSE kernel cache divergence — no synthetic trigger |
| Byte mismatch after size match → force write | ⚠️ Untestable | Same — requires FUSE stale read |
| Post-write hash mismatch | ⚠️ Untestable | Requires write-then-stale-read scenario |

#### `ContentEqual` — Pure function, Medium risk

| Path | Status |
|------|--------|
| Hash match | ✅ |
| Hash mismatch | ✅ |
| File not found → `(false, nil)` | ✅ |
| File unreadable (non-NotExist error) → `(false, err)` | ❌ **Gap — FIXED** |

#### `sizesDiffer` — Internal helper, Low risk

Package-private. The `false` path when `os.Stat` fails is covered transitively when src/dst don't exist. The gap is intentional — the function is a pure defensive guard.

#### `CopyFile` — I/O boundary, Medium risk

The 71.1% gap is entirely in atomic-write error paths (temp file creation, Sync, Chmod, Rename failures). These require fault-injection or mock filesystem — not worth the abstraction cost for this function.

#### `ComposeUpMultiple` / `resolveDeployMode`

The `detectNameConflicts` and name-conflict warning path in `ComposeUpMultiple` requires a live Docker daemon returning a specific stderr — integration-only. The `resolveDeployMode` secrets-fallback warning was already exercised by the table test; added an explicit named test isolating the no-appdata + secrets-only remote scenario.

**Anti-pattern check:** No assertionless tests, no tautological assertions, no swallowed errors, no real clocks, no sleeps found in changed test areas.

---

### Part 2: Narrative Logging Added

#### `internal/fileutil/fileutil.go` — `CopyFileIfChanged`

Three-phase narrative using `zerolog` at Debug level (not Warn — these are routine operations):

1. **Before check** (line ~163): `"Content hash matched, verifying file content before skip"` — fires when hash equality triggers the FUSE guard sequence
2. **Staleness detected** (existing Warn, message updated): `"FUSE staleness detected: content hash matched but file sizes differ, forcing write"` — actionable, searchable
3. **Staleness detected (bytes)**: `"FUSE staleness detected: content hash matched but byte comparison differs, forcing write"`
4. **Skip confirmed** (new): `"Content verified, skip confirmed"` — closes the narrative loop when all checks pass
5. **Post-write before**: `"Post-write verification: re-reading destination hash"` — marks the start of the verification step
6. **Post-write success** (new): `"Post-write verification: destination hash confirmed"` — previously missing; only failure paths were logged

#### `internal/reconcile/reconcile.go` — `resolveDeployMode`

Added structured Warn log before `ui.Warning` for the implicit-remote path:

```go
logger.Warn().
    Str("resolved_via", "secrets").
    Str("secrets_key", "network.unraid_ip").
    Str("recommendation", "set target_host in bosun.yaml or BOSUN_DEPLOY_MODE env var").
    Msg("Remote deploy mode resolved from secrets fallback — explicit config recommended for predictable behavior")
```

Decision path is now machine-readable: `resolved_via=secrets` distinguishes this from explicit `target_host` or `BOSUN_DEPLOY_MODE=remote` paths, making log queries actionable.

---

### New Tests Added

- `fileutil_test.go`: `TestCopyFileIfChanged/symlink_source_skips_gracefully` — verifies `(false, nil)` when src is a symlink
- `fileutil_test.go`: `TestContentEqual/returns_error_when_path_is_unreadable` — covers the non-NotExist error branch; skips under root
- `reconcile_test.go`: `TestResolveDeployMode/implicit_secrets-only_remote_with_no_appdata_configured` — isolates the no-appdata + secrets-only case that triggers the new structured Warn log

