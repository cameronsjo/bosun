// Single source of truth for the GitHub Pages base path. Imported by
// astro.config.mjs (the `base` config option) and scripts/sync-docs.mjs
// (which bakes it into the internal links it rewrites), so the two never
// drift out of sync.
export const SITE = 'https://cameronsjo.github.io';
export const BASE = '/bosun';
